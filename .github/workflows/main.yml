name: AnyDesk Direct - by @lucchese

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  rdp-lucchese:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Turbo Mode & Optimization - @lucchese
        shell: pwsh
        run: |
          Write-Host "Otimizando sistema..."
          Set-MpPreference -DisableRealtimeMonitoring $true -DisableBehaviorMonitoring $true -DisableIOAVProtection $true -DisableScriptScanning $true -SubmitSamplesConsent 2
          $services = @("DiagTrack", "wscsvc", "SearchIndexer", "SysMain", "WerSvc")
          foreach ($svc in $services) {
              if (Get-Service $svc -ErrorAction SilentlyContinue) {
                  Stop-Service $svc -Force -ErrorAction SilentlyContinue
                  Set-Service $svc -StartupType Disabled
              }
          }
          powercfg /s 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
          Write-Host "Turbo Mode ativado!"

      - name: Install & Config AnyDesk - @lucchese
        shell: pwsh
        run: |
          Write-Host "Baixando AnyDesk..."
          $url = "https://download.anydesk.com/AnyDesk.exe"
          $exePath = "$env:TEMP\AnyDesk.exe"
          Invoke-WebRequest -Uri $url -OutFile $exePath
          
          Write-Host "Iniciando AnyDesk em modo servico..."
          # Inicia o AnyDesk direto do EXE para garantir que ele esteja rodando
          Start-Process -FilePath $exePath -ArgumentList "--service" -WindowStyle Hidden
          Start-Sleep -Seconds 5
          
          # Gerar senha aleatória
          $pass = -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 10 | % {[char]$_})
          echo "ANYDESK_PASSWORD=$pass" >> $env:GITHUB_ENV
          
          Write-Host "Configurando senha de acesso..."
          echo $pass | & $exePath --set-password
          Start-Sleep -Seconds 5
          
          # Salva o caminho para o proximo passo
          echo "AD_PATH=$exePath" >> $env:GITHUB_ENV

      - name: Get AnyDesk ID - @lucchese
        shell: pwsh
        run: |
          $ad = $env:AD_PATH
          $id = ""
          $maxAttempts = 20
          $attempt = 0
          
          Write-Host "Obtendo ID do AnyDesk (isso pode levar um minuto)..."
          while (-not $id -and $attempt -lt $maxAttempts) {
              $id = & $ad --get-id
              if ($id) { 
                  $id = $id.Trim()
                  # Verifica se o ID contem apenas numeros (formato valido)
                  if ($id -match '^\d+$') { break } else { $id = "" }
              }
              Start-Sleep -Seconds 5
              $attempt++
              Write-Host "Tentativa $($attempt)/$maxAttempts..."
          }
          
          if (-not $id) {
              Write-Host "Tentando metodo alternativo para obter ID..."
              $id = & $ad --get-alias
              if ($id) { $id = $id.Trim() }
          }
          
          if (-not $id) {
              Write-Error "Nao foi possivel gerar o ID. Verifique se a rede da VM esta ativa."
              exit 1
          }
          
          echo "ANYDESK_ID=$id" >> $env:GITHUB_ENV
          Write-Host "AnyDesk ID: $id"

      - name: Final Credentials - @lucchese
        run: |
          $id = $env:ANYDESK_ID
          $pass = $env:ANYDESK_PASSWORD
          
          Write-Host "`n=== ANYDESK ACCESS || @luccheselindo ==="
          Write-Host "ID: $id"
          Write-Host "Password: $pass"
          Write-Host "=====================================`n"
          
          $summary = @"
          ### Acesso AnyDesk - @lucchese
          - **ID:** `$id`
          - **Senha:** `$pass`
          "@
          $summary | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append -Encoding utf8
          
          while ($true) {
              Write-Host "[$(Get-Date)] AnyDesk Online - ID: $id"
              Start-Sleep -Seconds 300
          }
